{
  "name": "path-scanner",
  "version": "1.0.0",
  "description": "Lightweight web crawler for path discovery and form extraction",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "path-scanner": "dist/index.js"
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "xml2js": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/xml2js": "^0.4.14",
    "tsdown": "^0.9.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
